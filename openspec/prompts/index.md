# 杂项提示词

本项目全部的杂项提示词。

## 001 初始化 `origin-to-pure-admin` 的 openspec 任务

你的任务是阅读文档要求，帮我初始化一揽子 openspec 任务。任务名称为 `origin-to-pure-admin` 。

1. 阅读 `openspec\prompts\2025-12-26-origin-to-pure-admin` 目录内的全部文件。
2. 严格按照文档要求，生成满足 openspec 规格的任务。

## 002 重做状态栏

/statusline 重做状态栏

1. 阅读 `D:\code\github-desktop-store\gh.10wms\.claude\statusline.sh` 文件。
2. 现在的上下文窗口额度的写法，计算出来的值竟然是 `N/A` ，这是无意义的错误值。我必须要在 claude code 的状态栏内，看到有效的，有意义的上下文窗口余量。
3. 请你大胆的改写原来的写法。重新的写一套。不需要考虑兼容性的事情。
